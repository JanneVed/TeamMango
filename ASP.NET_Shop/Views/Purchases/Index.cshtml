@model IEnumerable<ASP.NET_Shop.Models.Purchase>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Buyer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Buyer)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BuyerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BuyerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BuyerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
